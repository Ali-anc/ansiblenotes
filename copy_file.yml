---
- hosts: all 
  become_user: root
  tasks:
  - name: test transfer
    copy:
      src: /tmp/testfile
      dest: /home/achad
      remote_src: no
      owner: achad
      group: achad
      mode: '0644'
